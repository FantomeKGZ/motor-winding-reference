# Сопоставление данных двигателя со схемами обмотки

## Основной принцип

Схема укладки и схема соединения — самостоятельные справочные сущности. Они не принадлежат одной конкретной модели двигателя и могут подходить нескольким двигателям.

Поэтому связь строится так:

`рабочие данные ESP32 → параметры обмотки → поиск кандидатов во внешнем справочнике → выбранная схема`

## Текущее состояние алгоритма

Первая рабочая ступень уже реализована в `site/shared/js/esp32-scheme-matcher.js`.

Сейчас алгоритм:

1. читает реальные ссылки из загруженной оригинальной главной таблицы;
2. определяет число пазов из строки таблицы;
3. читает обороты из атрибута `title` исходной ссылки;
4. при наличии читает `q` из того же исходного `title`;
5. выполняет обязательное точное совпадение `slots + rpm`;
6. если ESP32 передала `q` и в исходной ссылке оно указано, дополнительно требует совпадение `q`;
7. подсвечивает подходящую группу и показывает кандидатов пользователю;
8. никогда не выбирает конкретную укладку автоматически только по этим параметрам.

Важно: обороты берутся именно из метаданных исходной ссылки. В оригинальной таблице внутри `<a>` обычно находится только изображение, поэтому `textContent` ссылки не является надёжным источником оборотов.

## Входные параметры

Минимально необходимый набор для текущего автоматического поиска:

- `slots` — число пазов;
- `rpm` — обороты/скоростная группа.

Дополнительный уже поддерживаемый критерий:

- `q` — число пазов на полюс и фазу, если оно известно ESP32 и присутствует в метаданных исходной ссылки.

Параметры для следующих ступеней фильтрации:

- `poles` — число полюсов/`2p`;
- `phases` — число фаз;
- `winding_type` — тип обмотки;
- `parallel_branches` — число параллельных ветвей;
- `connection` — звезда/треугольник/переключаемая схема;
- дополнительные признаки для специальных и многоскоростных машин.

Сайт не должен выводить отсутствующие технические параметры из косвенных чисел имени файла или текста страницы, если это не зафиксировано отдельным надёжным правилом.

## Многоступенчатая схема поиска

Целевая последовательность:

1. обязательная группа `slots + rpm`;
2. `q`, если доступно с обеих сторон;
3. параметры внутри страницы схемы — `2p`, тип укладки и другие безопасно извлекаемые признаки;
4. параллельные ветви;
5. тип соединения;
6. специальные признаки многоскоростных и нестандартных обмоток.

Если некоторого дополнительного параметра нет в справочнике или ESP32, он не должен выдумываться и не должен автоматически исключать кандидата.

## Результат поиска

Поиск может вернуть:

1. одно совпадение по доступным критериям — показать его как кандидата, но перед применением оставить техническую проверку;
2. несколько кандидатов — показать мастеру список с отличиями;
3. нет совпадений — оставить исходные параметры и предложить ручной поиск в привычном справочнике.

Нельзя автоматически выбирать схему только по имени модели двигателя.

## Контрольные тесты

`site/shared/data/esp32-mock.json` содержит положительные и отрицательные случаи. Тестовый режим `?esp32test=1` проверяет не просто наличие результата, а ограничения `expected.min_matches` и `expected.max_matches`.

В матрицу входят проверки корректного `q` и отдельный отрицательный случай с неверным `q`, чтобы дополнительный фильтр не давал ложное совпадение.

## Идентификатор схемы

После инвентаризации каждой самостоятельной схеме можно присвоить стабильный `scheme_id`. На первом этапе `scheme_id` живёт только во внешнем сайте. Позже выбранный идентификатор может сохраняться в записи проекта на ESP32 как ссылка, а не как копия схемы.

## Правила для изображений

- оригинальные изображения не удалять;
- одинаковое имя файла не доказывает одинаковое содержание;
- одинаковый размер не доказывает одинаковое содержание;
- точное совпадение хэша доказывает одинаковые байты, но прежде чем объединять ссылки, всё равно проверить контекст использования;
- изображения с любым графическим отличием считать разными;
- в финальном реестре хранить список страниц, использующих изображение.

## Совместимость со старым интерфейсом

Новый подбор схем является дополнительной возможностью. Оригинальная таблица «число пазов × обороты» и привычные переходы должны оставаться доступными независимо от работы автоматического сопоставления.
